module.exports = (client) => {
    client.slash = {
        commands = {
            // check if the command exists
            get: async () => {

            },
            // run the command
            run: async () => {

            }
        }
    }
}